# Ascendant Digital storefront runtime.
# Pinned to a floor that supports the current Starlette TemplateResponse
# signature — the legacy (name, context) form breaks silently on upgrade.
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9
stripe>=8.0
psycopg2-binary>=2.9
python-dotenv>=1.0
filelock>=3.13
