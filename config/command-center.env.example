# Copy to: ~/.config/dominion/command-center.env
# Then run: chmod 600 ~/.config/dominion/command-center.env
# Never commit the real values.

# Primary Dominion intelligence worker.
# Recovery hold: keep NEMOTRON_BASE_URL empty until a separately reviewed,
# Founder-authorized Nemotron activation has fresh runtime acceptance evidence.
NEMOTRON_BASE_URL=
NEMOTRON_MODEL=nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B
NEMOTRON_API_KEY=

# Governed fallback. Production Command Center uses host networking while
# binding its own HTTP surface to 127.0.0.1:8091, so private host-loopback
# Conductor remains reachable without opening port 5060 publicly.
CONDUCTOR_URL=http://127.0.0.1:5060
INTELLIGENCE_TIMEOUT_SECONDS=60

# First revenue-ready offer: The Art of True Healing — $27
# CHECKOUT must be the verified live payment URL.
# DELIVERY must be the verified private delivery workflow or protected file target.
CHECKOUT_ART_OF_TRUE_HEALING=
DELIVERY_ART_OF_TRUE_HEALING=

# Keep later offers blocked until individually tested.
CHECKOUT_CREDIT_DISPUTE_TOOLKIT=
DELIVERY_CREDIT_DISPUTE_TOOLKIT=
CHECKOUT_AI_AUTOMATION_BLUEPRINT=
DELIVERY_AI_AUTOMATION_BLUEPRINT=

VAULT_PATH=/home/malachisingleton8/vault
COMMAND_CENTER_MEMORY_PATH=/home/malachisingleton8/dominion-memory

# --- Read-only Alpaca PAPER bridge (additive, governed) ---
# Phone-facing auth for /api/trading/paper/*. Use 32+ random characters.
# Leave empty to keep the trading surface closed (it returns HTTP 503).
COMMAND_CENTER_API_KEY=

# Alpaca PAPER credentials. Server-side only; never returned in any response
# and never committed. Use a PAPER key pair — a live pair is refused by the
# host allow-list and must not be placed on the VM at all.
ALPACA_PAPER_KEY_ID=
ALPACA_PAPER_SECRET_KEY=
ALPACA_PAPER_BASE_URL=https://paper-api.alpaca.markets

# Existing Dominion Alpha paper engine, observed read-only for bot health.
ALPHA_ENGINE_BASE_URL=http://127.0.0.1:8787

# Append-only, hash-chained observation evidence.
COMMAND_CENTER_LEDGER_DIR=/home/malachisingleton8/.dominion/ledger
TRADING_SNAPSHOT_TTL_SECONDS=15
