version: "3.9"

services:
  dominion-command-center:
    build: ./apps/command-center
    container_name: dominion-command-center
    ports:
      - "127.0.0.1:8091:8000"
    volumes:
      - ${VAULT_PATH:-/home/malachisingleton8/vault}:/vault:ro
      - ${COMMAND_CENTER_MEMORY_PATH:-/home/malachisingleton8/dominion-memory}:/data/memory
      - ./governance/lane_access_policy.json:/governance/lane_access_policy.json:ro
    restart: unless-stopped
    environment:
      VAULT_PATH: /vault
      MEMORY_PATH: /data/memory
      LANE_POLICY_PATH: /governance/lane_access_policy.json
      NEMOTRON_BASE_URL: ${NEMOTRON_BASE_URL:-}
      NEMOTRON_MODEL: ${NEMOTRON_MODEL:-nemotron-3}
      NEMOTRON_API_KEY: ${NEMOTRON_API_KEY:-}
      CONDUCTOR_URL: ${CONDUCTOR_URL:-http://host.docker.internal:5060}
      INTELLIGENCE_TIMEOUT_SECONDS: ${INTELLIGENCE_TIMEOUT_SECONDS:-60}
      CHECKOUT_ART_OF_TRUE_HEALING: ${CHECKOUT_ART_OF_TRUE_HEALING:-}
      DELIVERY_ART_OF_TRUE_HEALING: ${DELIVERY_ART_OF_TRUE_HEALING:-}
      CHECKOUT_CREDIT_DISPUTE_TOOLKIT: ${CHECKOUT_CREDIT_DISPUTE_TOOLKIT:-}
      DELIVERY_CREDIT_DISPUTE_TOOLKIT: ${DELIVERY_CREDIT_DISPUTE_TOOLKIT:-}
      CHECKOUT_AI_AUTOMATION_BLUEPRINT: ${CHECKOUT_AI_AUTOMATION_BLUEPRINT:-}
      DELIVERY_AI_AUTOMATION_BLUEPRINT: ${DELIVERY_AI_AUTOMATION_BLUEPRINT:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD-SHELL", "python -c 'import urllib.request; urllib.request.urlopen(\"http://localhost:8000/health\").read()' || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
