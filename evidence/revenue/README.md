# Revenue Evidence

Runtime evidence in this directory supports Dominion's canonical revenue-learning loop. A repository artifact may document a finding or pre-merge repair, but it must not claim runtime completion unless the named runtime receipt was actually produced by the deployed system.

Required completion receipts are governed by `governance/integrity_installation_contract.json`.
