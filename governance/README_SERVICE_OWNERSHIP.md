# Service Ownership Registry

Canonical files:

- `service_ownership_registry.schema.json` — machine-readable contract.
- `service_ownership_registry.json` — runtime inventory and ownership records.
- `SERVICE_OWNERSHIP_IMPLEMENTATION.md` — safe reconciliation and enforcement sequence.

The production VM is runtime evidence. GitHub is the versioned control source. Conflicts remain visible and fail closed until reviewed and reconciled.
